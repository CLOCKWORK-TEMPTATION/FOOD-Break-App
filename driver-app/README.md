# BreakApp Driver App - تطبيق السائق

تطبيق React Native مخصص للسائقين لإدارة وتتبع عمليات التوصيل.

## الميزات الرئيسية / Key Features

### ✅ المُنفذة / Implemented
- 📱 **واجهة مستخدم كاملة** - Complete UI with React Native
- 🗺️ **تتبع GPS فوري** - Real-time GPS tracking
- 🔌 **WebSocket للتحديثات الفورية** - Real-time updates via WebSocket
- 📋 **إدارة التوصيلات النشطة** - Active deliveries management
- 🧭 **الملاحة إلى موقع التوصيل** - Navigation to delivery location
- 💰 **تتبع الأرباح** - Earnings tracking
- 📊 **سجل التوصيلات** - Delivery history
- 📸 **تأكيد التوصيل بالصورة** - Photo confirmation
- 🔔 **إشعارات فورية** - Push notifications
- 🔐 **تسجيل دخول آمن** - Secure authentication

## البنية / Structure

```
driver-app/
├── src/
│   ├── screens/          # شاشات التطبيق
│   │   ├── LoginScreen.tsx
│   │   ├── ActiveDeliveriesScreen.tsx
│   │   ├── DeliveryDetailScreen.tsx
│   │   ├── NavigationScreen.tsx
│   │   ├── EarningsScreen.tsx
│   │   └── ProfileScreen.tsx
│   ├── components/       # المكونات القابلة لإعادة الاستخدام
│   ├── services/         # خدمات API و WebSocket
│   │   ├── apiService.ts
│   │   ├── websocketService.ts
│   │   └── locationService.ts
│   ├── navigation/       # إعدادات التنقل
│   ├── theme/            # الألوان والأنماط
│   └── utils/            # دوال مساعدة
├── App.tsx               # نقطة الدخول
├── package.json
└── app.json
```

## التثبيت / Installation

```bash
# تثبيت التبعيات
cd driver-app
npm install

# تشغيل التطبيق
npm start

# تشغيل على Android
npm run android

# تشغيل على iOS
npm run ios
```

## المتطلبات / Requirements

- Node.js 18+
- Expo CLI
- Android Studio (للتطوير على Android)
- Xcode (للتطوير على iOS)

## الإعدادات / Configuration

قم بإنشاء ملف `.env` في مجلد التطبيق:

```env
EXPO_PUBLIC_API_URL=http://localhost:3001/api/v1
EXPO_PUBLIC_SOCKET_URL=http://localhost:3001
```

## الميزات التفصيلية / Detailed Features

### 1. شاشة التوصيلات النشطة
- عرض جميع التوصيلات النشطة
- قبول/رفض التوصيلات
- تحديثات فورية عبر WebSocket
- تفاصيل كل طلب (المطعم، العميل، العنوان، المبلغ)

### 2. شاشة الملاحة
- خريطة تفاعلية
- مسار من موقع السائق إلى موقع التوصيل
- تحديث الموقع كل 5 ثوانٍ
- حساب الوقت المتوقع للوصول (ETA)

### 3. تتبع الموقع
- تتبع GPS دقيق
- إرسال الموقع للخادم كل 5 ثوانٍ
- دعم التتبع في الخلفية
- حساب السرعة والاتجاه

### 4. إدارة حالة التوصيل
- تحديث حالة الطلب (قيد التحضير، في الطريق، تم التوصيل)
- إشعارات فورية للعميل عند كل تحديث
- تأكيد التوصيل بالصورة

### 5. الأرباح
- عرض الأرباح اليومية/الأسبوعية/الشهرية
- تفاصيل كل توصيلة
- إحصائيات الأداء

## التكامل مع Backend

يتصل التطبيق مع backend عبر:
1. **REST API** - لجلب البيانات والمصادقة
2. **WebSocket** - للتحديثات الفورية
3. **Expo Push Notifications** - للإشعارات

## الأمان / Security

- JWT للمصادقة
- تشفير البيانات الحساسة
- التحقق من الصلاحيات
- حماية من هجمات MITM

## الاختبار / Testing

```bash
# تشغيل الاختبارات
npm test
```

## النشر / Deployment

### Android
```bash
expo build:android
```

### iOS
```bash
expo build:ios
```

## الصيانة / Maintenance

- تحديث التبعيات بانتظام
- مراقبة الأداء
- معالجة تقارير الأخطاء
- تحسين استهلاك البطارية

## المساهمة / Contributing

للمساهمة في تطوير التطبيق:
1. Fork المشروع
2. أنشئ branch جديد
3. قم بالتعديلات
4. أرسل Pull Request

## الدعم / Support

للدعم الفني، تواصل معنا على:
- Email: support@breakapp.com
- GitHub Issues: [Report Issue](https://github.com/breakapp/issues)

---

تم التطوير بواسطة فريق BreakApp ❤️
