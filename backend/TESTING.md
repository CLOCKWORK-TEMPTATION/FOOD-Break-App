# 🧪 دليل الاختبارات الشامل - BreakApp Backend

## نظرة عامة

تم إعداد نظام اختبارات شامل يغطي ثلاثة مستويات:
1. **Unit Tests** - اختبارات الوحدات للخدمات الفردية
2. **Integration Tests** - اختبارات التكامل لـ API Endpoints
3. **E2E Tests** - اختبارات End-to-End للسيناريوهات الكاملة

## 📊 إحصائيات الاختبارات

| النوع | عدد الاختبارات | الحالة |
|-------|---------------|--------|
| Unit Tests | 115 | ✅ نجاح |
| Integration Tests | 100 | ✅ نجاح |
| E2E Tests | 63 | ✅ نجاح |
| **المجموع** | **278** | ✅ نجاح |

## 📁 هيكل مجلدات الاختبارات

```
backend/tests/
├── fixtures/
│   └── testData.js          # بيانات الاختبار الثابتة
├── helpers/
│   └── testHelpers.js       # دوال مساعدة للاختبارات
├── unit/
│   └── services/
│       ├── authService.test.js      # اختبارات المصادقة
│       ├── orderService.test.js     # اختبارات الطلبات
│       ├── menuService.test.js      # اختبارات القوائم
│       ├── paymentService.test.js   # اختبارات المدفوعات
│       └── qrCodeService.test.js    # اختبارات QR Code
├── integration/
│   └── api/
│       ├── auth.test.js             # اختبارات API المصادقة
│       ├── orders.test.js           # اختبارات API الطلبات
│       └── admin.test.js            # اختبارات API الإدارة
├── e2e/
│   ├── userJourney.test.js          # رحلة المستخدم الكاملة
│   └── adminJourney.test.js         # رحلة المسؤول الكاملة
└── utils/
    └── jwt.test.js                  # اختبارات JWT
```

## 🚀 تشغيل الاختبارات

### تشغيل جميع الاختبارات
```bash
npm test
```

### تشغيل اختبارات محددة
```bash
# اختبارات الوحدات فقط
npm run test:unit

# اختبارات التكامل فقط
npm run test:integration

# اختبارات E2E فقط
npm run test:e2e
```

### تشغيل مع المراقبة
```bash
npm run test:watch
```

### تشغيل مع تقرير التغطية
```bash
npm run test:coverage
```

### تشغيل للـ CI/CD
```bash
npm run test:ci
```

## 📋 تفاصيل الاختبارات

### Unit Tests - اختبارات الوحدات

#### Auth Service (20 اختبار)
- التسجيل الناجح
- رفض التسجيل ببريد موجود
- تشفير كلمات المرور
- تسجيل الدخول الناجح
- رفض كلمة المرور الخاطئة
- التحقق من Token
- رفض Token منتهي الصلاحية
- تغيير كلمة المرور
- التحقق من الأدوار (ADMIN, PRODUCER, REGULAR, VIP)

#### Order Service (26 اختبار)
- إنشاء طلب صالح
- رفض طلب لمطعم غير نشط
- رفض أصناف غير متاحة
- حساب المجموع الصحيح
- تحديث حالة الطلب
- إلغاء الطلب
- التحقق من صحة البيانات
- تطبيق الضريبة والخصومات

#### Menu Service (22 اختبار)
- جلب عناصر القائمة
- الفلترة حسب الفئة
- البحث بالاسم
- إنشاء عنصر جديد
- تحديث الأسعار
- تبديل التوفر
- دعم القيود الغذائية

#### Payment Service (23 اختبار)
- إنشاء Payment Intent
- معالجة الدفع الناجح
- معالجة فشل الدفع
- استرداد كامل/جزئي
- التحقق من المبالغ
- دعم عدة مزودين (Stripe, PayPal)

#### QR Code Service (24 اختبار)
- توليد رمز QR
- توليد Token مشفر
- التحقق من Token صالح
- رفض Token منتهي
- ربط المستخدم بالمشروع
- دعم صيغ متعددة (PNG, SVG)
- الأمان والتشفير

### Integration Tests - اختبارات التكامل

#### Auth API (31 اختبار)
- POST /api/v1/auth/register
- POST /api/v1/auth/login
- POST /api/v1/auth/refresh
- POST /api/v1/auth/logout
- POST /api/v1/auth/forgot-password
- GET /api/v1/auth/profile
- PUT /api/v1/auth/profile
- PUT /api/v1/auth/change-password
- Rate Limiting
- Security Headers

#### Orders API (36 اختبار)
- POST /api/v1/orders
- GET /api/v1/orders
- GET /api/v1/orders/:id
- PUT /api/v1/orders/:id/status
- POST /api/v1/orders/:id/cancel
- POST /api/v1/orders/:id/reorder
- GET /api/v1/orders/:id/track
- Admin Order Management
- Pagination & Filtering

#### Admin API (33 اختبار)
- GET /api/v1/admin/stats
- GET /api/v1/admin/orders
- PUT /api/v1/admin/orders/:id/status
- GET /api/v1/admin/restaurants
- PUT /api/v1/admin/restaurants/:id/toggle
- GET /api/v1/admin/menu
- PUT /api/v1/admin/menu/:id
- GET /api/v1/admin/users
- POST /api/v1/admin/notifications/send
- Authorization & Audit

### E2E Tests - اختبارات End-to-End

#### User Journey (33 اختبار)
**Scenario 1: تسجيل جديد والطلب الأول**
1. تسجيل المستخدم
2. تصفح المطاعم القريبة
3. اختيار مطعم
4. عرض القائمة
5. إضافة للسلة
6. تقديم الطلب
7. الدفع
8. تأكيد الطلب
9. تتبع الطلب
10. استلام الطلب

**Scenario 2: مسح QR Code**
1. مسح الرمز
2. التحقق من Token
3. التحقق من المشروع
4. ربط المستخدم
5. عرض القائمة
6. تقديم طلب ضمن الوقت المحدد

**Scenario 3: إعادة الطلب السريع**
1. تسجيل الدخول
2. عرض السجل
3. اختيار طلب سابق
4. التحقق من التوفر
5. إنشاء طلب جديد

**Scenario 4: إلغاء الطلب**
1. عرض الطلب
2. طلب الإلغاء
3. استرداد المبلغ
4. إشعار الإلغاء

**Scenario 5: إدارة الحساب**
1. عرض الملف الشخصي
2. تحديث البيانات
3. تغيير كلمة المرور
4. تفضيلات غذائية

#### Admin Journey (30 اختبار)
**Scenario 1: العمليات اليومية**
1. تسجيل دخول المسؤول
2. عرض الإحصائيات
3. مراجعة الطلبات المعلقة
4. تأكيد الطلبات
5. عرض المستخدمين
6. إرسال إشعارات

**Scenario 2: إدارة المطاعم**
1. إضافة مطعم جديد
2. إضافة عناصر القائمة
3. تحديث الأسعار
4. تعطيل عنصر
5. تعطيل مطعم مؤقتاً
6. إعادة تفعيل مطعم

**Scenario 3: حل مشاكل الطلبات**
1. استلام شكوى
2. عرض تفاصيل الطلب
3. استرداد جزئي
4. إضافة رصيد تعويضي
5. إرسال اعتذار
6. إغلاق المشكلة

**Scenario 4: عمليات الإنتاج**
1. تسجيل دخول المنتج
2. عرض الطلبات المسندة
3. بدء التحضير
4. تجهيز الطلب
5. تسليم للتوصيل

**Scenario 5: التقارير**
1. تقرير يومي
2. أفضل المطاعم
3. الأصناف الشائعة
4. تصدير CSV

## 🔧 إعداد بيئة الاختبار

ملف `jest.setup.js` يحتوي على:
- متغيرات البيئة للاختبار
- Mock لـ Prisma Client
- Mock لـ Console methods
- Helper functions عامة

## 📦 البيانات التجريبية (Fixtures)

ملف `fixtures/testData.js` يحتوي على:
- **users**: مستخدمين بأدوار مختلفة
- **restaurants**: مطاعم نشطة وغير نشطة
- **menuItems**: عناصر متاحة وغير متاحة
- **orders**: طلبات بحالات مختلفة
- **payments**: مدفوعات مكتملة ومعلقة
- **projects**: مشاريع QR نشطة

## 🔐 اختبارات الأمان

الاختبارات تغطي:
- ✅ تشفير كلمات المرور (bcrypt)
- ✅ التحقق من JWT
- ✅ رفض Token منتهي
- ✅ حماية Endpoints حسب الدور
- ✅ عدم كشف passwordHash
- ✅ Rate Limiting
- ✅ Audit Logging

## 🔄 التكامل مع CI/CD

للتشغيل في CI/CD:
```bash
npm run test:ci
```

هذا الأمر:
- يشغل جميع الاختبارات
- ينشئ تقرير تغطية
- ينتج ملف junit.xml للتقارير

## ⚠️ ملاحظات مهمة

1. **الاختبارات تستخدم Mocks**: يتم محاكاة Prisma Client لتجنب الاعتماد على قاعدة بيانات حقيقية.

2. **بيئة الاختبار**: يتم ضبط `NODE_ENV=test` تلقائياً.

3. **Timeout**: الاختبارات لديها timeout مدته 15 ثانية.

4. **تغطية الكود**: التغطية الحالية تعكس اختبارات Mock. للحصول على تغطية حقيقية، يلزم اختبارات integration مع قاعدة بيانات فعلية.

## 🎯 التوسع المستقبلي

للتوسع في الاختبارات:
1. إضافة اختبارات integration مع قاعدة بيانات اختبار
2. إضافة اختبارات performance
3. إضافة اختبارات load testing
4. تغطية المزيد من الخدمات (notification, nutrition, etc.)
